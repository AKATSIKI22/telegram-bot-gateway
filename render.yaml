services:
  - type: web
    name: telegram-bot-gateway
    runtime: python
    repo: https://github.com/AKATSIKI22/telegram-bot-gateway
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_CHAT_ID
        sync: false
      - key: SITE_URL
        sync: false

  - type: worker
    name: telegram-bot-worker
    runtime: python
    repo: https://github.com/AKATSIKI22/telegram-bot-gateway
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_CHAT_ID
        sync: false
      - key: SITE_URL
        sync: false
